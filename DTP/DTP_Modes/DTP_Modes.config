!=========================================
! VTP (VLAN Trunking Protocol) Modes Configuration
!=========================================

!=========================================
! SWITCH 1: CLIENT MODE CONFIGURATION
!=========================================

hostname Client-Switch

! Client mode configuration
vtp mode client
vtp version 2
vtp domain cisco.com

! IMPORTANT: Client mode switches CANNOT create or modify VLANs
! This attempted VLAN creation will NOT work in client mode
! vlan 20 (will not work because client mode is read-only)

! VLANs are automatically learned from VTP server switches
! Verify received VLANs with: show vlan brief


!=========================================
! SWITCH 2: TRANSPARENT MODE CONFIGURATION
!=========================================

hostname Transparent-Switch

! Transparent mode configuration
vtp mode transparent
vtp version 2
vtp domain cisco.com

! Transparent mode switches maintain independent VLAN database
! They do NOT learn VLANs from servers
! They do NOT sync with other switches

! Local VLAN creation works in transparent mode
vlan 30
  name Security-Team

! These VLANs only exist on this switch
! Other switches do NOT receive this VLAN


!=========================================
! SWITCH 3: SERVER MODE CONFIGURATION
!=========================================

hostname Server-Switch

! By default, all switches start in SERVER mode
! Server mode is used for VLAN management and distribution

vtp mode server
vtp version 2
vtp domain cisco.com

! Create VLANs on server - they will be distributed to clients
vlan 10
  name HR

! Verify VTP status and VLAN database
! show vtp status
! show vlan brief


!=========================================
! IMPORTANT CONCEPTS
!=========================================

! Revision Number: Increments each time a VLAN is created/modified
! - Server has highest revision number
! - Used to determine which switch's VLAN database is authoritative
! - HIGH RISK: If old server has higher revision, it wins!

! VTP Domain: Logical grouping of switches
! - All switches in same domain synchronize VLAN information
! - Domain name is case-sensitive
! - Switches with different domains do NOT sync

! VTP Version: Protocol version support
! - Version 1: Standard (default)
! - Version 2: Adds Token Ring support


!=========================================
! VERIFICATION COMMANDS
!=========================================

! Show VTP status on any switch
show vtp status

! Display VLAN database and revision number
show vlan brief

! View VLAN details
show vlan id 10

! Check if VLANs are properly synchronized
show interfaces trunk

